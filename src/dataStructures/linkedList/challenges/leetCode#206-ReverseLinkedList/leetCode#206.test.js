const reverseList = require('./leetcode#206');

test('reverseList function exists', () => {
    expect(reverseList).toBeDefined();
});

// test('reverseList(2,3) should return 5', () => {
//     expect(reverseList('2', '3')).toEqual('5');
// });

